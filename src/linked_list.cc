#include <iostream>

#include "linked_list.h"

namespace algo{

  bool SLinkedList::isEmpty(){
    return is_empty;
  }

  bool SLinkedList::push_back(int num){
    is_empty = false;
    return is_empty;
  }
}